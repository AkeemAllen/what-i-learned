if [ -z `ssh-keygen -F 74.207.224.133` ]; then
  ssh-keyscan -H 74.207.224.133 >> ~/.ssh/known_hosts
fi

ssh akeem@74.207.224.133

echo "${password}"

echo "We made it"

echo "$(cat what-i-learned/package.json)"